spring:
  application:
    name: ditto

  profiles:
    default: local

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

  config:
    import: classpath:/application-secret.yml

---

spring:
  config:
    activate:
      on-profile: local, dev, prod